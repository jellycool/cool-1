extends layout

block content

  include addsearchuser

  h1= title
  p Welcome to #{title}
  table(border="3")
   tr
      th id
      th name
      th age
      th operation
      each user , index in userlist
       tr  
         td  
          a(href='/updateuser/#{user._id}') #{user._id}
         td=user.name 
         td=user.age
         td
          a(href='/deluser/#{user._id}') 删除       
  h1
    a(href='/userlist') 用户列表首页


       
     
         
     
    